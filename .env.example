# API Keys
OPENAI_API_KEY=
ALCHEMY_API_KEY=
INFURA_API_KEY=
NODIES_API_KEY=
ANKR_API_KEY=

# Trading / Wallet Config
PRIVATE_KEY=
PUBLIC_ADDRESS=
SOLIDITY_CONTRACT_ADDRESS=

# Network Settings
POLYGON_RPC_URL=
ETHEREUM_RPC_URL=
CHAIN_ID=

# Bot Settings
MIN_PROFIT_PCT=0.2
MAX_SLIPPAGE_PCT=0.03
TRADE_COOLDOWN=10

# API Bridge Settings
API_HOST=0.0.0.0
API_PORT=5050

# ArbiGirl Settings
ARBIGIRL_BRIDGE_URL=http://127.0.0.1:5050
ARBIGIRL_MODEL=gpt-4o-mini
ARBIGIRL_MIN_PROFIT_USD=1.0
ARBIGIRL_AUTO_EXECUTE=false
ARBIGIRL_LOG=arbigirl.log

# Wallet (for execution)
WALLET_PRIVATE_KEY=

# ============================================================================
# GRAPH-BASED ARBITRAGE AUTOMATION (ZERO CAPITAL RISK!)
# ============================================================================

# QUICK START: Just set these 3 things and run ./start-automation.sh
# 1. ALCHEMY_API_KEY - Get from alchemy.com (required)
# 2. PRIVATE_KEY - Your wallet private key (required)
# 3. CONTRACT_ADDRESS - Your flash loan contract (required)
# 4. AUTO_EXECUTE - Set to "true" to automatically execute trades

# Execution Control
AUTO_EXECUTE=false                  # SET TO "true" TO AUTO-EXECUTE (test mode first!)
MIN_TVL_USD=3000                    # Minimum pool TVL in USD

# Graph Pathfinding Settings
BASE_TOKENS=USDC,WETH,WPOL,DAI      # Starting tokens for multi-hop paths
TEST_AMOUNTS=1000,5000,10000        # Trade sizes to test (in USD)
SCAN_INTERVAL_SECONDS=60            # Seconds between scans

# Flash Loan Sizing (NO CAPITAL RISK - only gas costs!)
MIN_TRADE_SIZE_USD=1000             # Minimum flash loan size
MAX_TRADE_SIZE_USD=100000           # Maximum flash loan size
OPTIMAL_TRADE_SIZE_USD=15000        # Optimal size for most pools ($10k-$20k)

# Profit Requirements (gas is cheap on Polygon!)
MIN_PROFIT_AFTER_GAS=0.75           # Min profit after gas ($0.75)
MIN_PROFIT_AFTER_FEES=1.00          # Min after gas + flash loan fees ($1.00)

# Slippage & Liquidity
MAX_SLIPPAGE_PCT=3.0                # Max slippage (3% - higher ok since no capital risk!)
MIN_POOL_TVL=5000                   # Minimum pool liquidity

# Rate Limiting (aggressive - failures only cost gas!)
MAX_TRADES_PER_MINUTE=10            # Max 10 trades/minute
MAX_GAS_SPENT_PER_HOUR=5.0          # Max $5 gas/hour
COOLDOWN_SECONDS=0.1                # 100ms cooldown (almost instant!)

# Kill Switch (higher threshold - failures are cheap!)
KILL_ON_CONSECUTIVE_FAILURES=10     # Disable after 10 consecutive failures

# Flash Loan Provider
PREFER_BALANCER=true                # Balancer = 0% fees! (Aave = 0.09%)

# Smart Contract Addresses (Polygon)
CONTRACT_ADDRESS=                   # Your arbitrage contract address
ALCHEMY_URL1=                       # Primary Alchemy RPC URL

# Optional: Premium Alchemy key for separate billing/tracking
PREMIUM_ALCHEMY_KEY=                # Uses ALCHEMY_API_KEY if not set

# ============================================================================
# IMPORTANT: PRIVATE TRANSACTIONS (MEV PROTECTION)
# ============================================================================
# All transactions are AUTOMATICALLY sent via Alchemy's eth_sendPrivateTransaction
# This prevents:
#   - Frontrunning attacks
#   - Sandwich attacks
#   - MEV bots stealing your opportunities
#
# Uses PREMIUM_ALCHEMY_KEY if set, otherwise ALCHEMY_API_KEY
# No additional configuration needed - it just works!
#
# ============================================================================
# PROFITABILITY & EXPECTATIONS
# ============================================================================
# REALISTIC EXPECTATIONS:
#   - Arbitrage is highly competitive on Polygon
#   - Most bots find the same opportunities
#   - Profit margins are typically 0.1% - 0.5%
#   - You need: fast RPC, low latency, optimized code
#   - Graph method finds multi-hop paths others may miss
#
# ADVANTAGES OF THIS BOT:
#   ✓ Flash loans = zero capital risk (only gas costs)
#   ✓ Private transactions = MEV protection
#   ✓ Graph pathfinding = finds multi-hop opportunities
#   ✓ Auto-execution = no manual intervention
#   ✓ Safety limits = prevents excessive losses
#
# RECOMMENDED APPROACH:
#   1. Run with AUTO_EXECUTE=false first (observe opportunities)
#   2. Check logs to see frequency and size of opportunities
#   3. If profitable opportunities appear regularly, enable AUTO_EXECUTE
#   4. Start with low limits (MAX_GAS_SPENT_PER_HOUR=5.0)
#   5. Monitor for 24-48 hours before scaling up
#
# REALITY CHECK:
#   - This bot may NOT be immediately profitable
#   - Success depends on market conditions and competition
#   - Most value comes from learning and optimization
#   - Consider it a learning/research tool first
#
